{
  'use strict'

  console.log("this works")

  function switchFunc() {
    var ratingState = document.getElementById();
    var thankYouState = document.getElementById();
  
    ratingState.style.display = (
      mainFrameOne.style.display == "none" ? "block" : "none"); 
    thankYouState.style.display = (
      mainFrameTwo.style.display == "none" ? "block" : "none"); 
  }

}
